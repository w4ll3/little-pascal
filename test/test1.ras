/*
 * Programa contendo laço de repetição e estrutura condicional aninhados
 */

program fatorial;

var i, j, fac: integer;

begin
  read(j);
  i := 0;
  fac := 1;
  while (i < j) do
  begin
    write(i * i - 1)
    i := i + 1;
  end;
end.